/** allocation palette — the accent plus two neutrals; shared by donut + legend + cards */
export const ALLOC_COLORS: Record<string, string> = {
  crypto: '#C9F77E',
  stocks: '#A1A1AA',
  cash: '#3F3F46',
}
